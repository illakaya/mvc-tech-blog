{{!-- post title --}}
<div class="card-header d-flex justify-content-between align-items-center flex-container">
  <h3 class="flex-item">{{title}}</h3>
  <p class="flex-item my-0">Posted by {{user.username}} on date</p>
</div>
<div class="card-body">
  {{!-- post text, with the identified user who wrote the post --}}
  <p class="card-text">{{text}}</p>
</div>
